# NOREADME.md — AI 对话记录（仅用户发言）

## 对话 1 — 初始需求

> 请生成一个简单的绘画板程序，逻辑是：初始化1080x920窗口画板，程序端口为11451。等待40秒后开始由中点绘制最速降线，整个窗口的刷新率为60fps，创建边界并使cache始终跟随鼠标，如果移出则删除另一边的缓存并跟随最新绘制点移动边界。跟随鼠标绘制，除非停止移动10-39秒（随机秒）。如果鼠标开始移动则停止自动绘制，否则进行螺旋线绘制或者布朗运动。现在，请用Python编写需要的程序并import需要的库，测试并编译生成exe程序。

## 对话 2 — v1.1 修正

> 我的意思是，请自动创建程序得得得得得完整结构到一个DrawingBoard文件夹，并按GIthub要求生成开源程序结构和readme.md。严禁使用emoji和"不"字，包含作者qq号331701160，License为：MIT，附加："如果您让AI使用了本项目，请创建并将与AI对话的过程开源并写入'NOREADME.md'文件中（不含AI回复）。"，同时把对话放入NOREADME.md文件中。请修改一些细节病重新编译1.1：1.注意弄一个合理的阿基米德螺旋线尺寸，现在太大了，只看得见一个圆弧。2.除螺线、布朗运动模式外，添加一个绘制随机半径圆的模式，逆时针绘制，绘制速度大概为1cm/s，不要画太快。3.取消"等待40秒后开始由中点绘制最速降线"。4.改变逻辑为：等待后在鼠标人工手动画的线的最末端随机秒数后开始随机选择三种自动模式之一接着画。

## 对话 3 — v1.2 修正

> 现在的情况是：自动绘制的内容会自动清除，请不要自动清除。添加按键p功能：随机一个画笔的颜色。

## 对话 4 — v1.3 渐变与 777 机制

> 增加一个画笔渐变按键o：从上一个颜色过渡到下一个颜色时有10cm长度的渐变。并添加777！机制，有7.77%的概率在鼠标连续移动10秒后立刻随机一个颜色并开始现在的画笔（不管是自动还是手动）的渐变。
